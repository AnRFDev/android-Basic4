<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rustfisher.basic4">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- 搜索WiFi获取SSID需要的权限 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.Hts.SAct1" />
        <activity android:name=".activity.Hts.SAct2" />
        <activity android:name=".activity.Hts.SAct3" />
        <activity android:name=".activity.ViewLifecycleAct" />
        <activity android:name=".activity.IntentTestAct" />
        <activity android:name=".activity.HandlerDemoActivity" />
        <activity android:name=".activity.AnnotationDemoActivity" />
        <activity android:name=".activity.ForceNetworkActivity" />
        <activity android:name=".wifiscan.WiFiScanActivity" />
        <activity android:name=".activity.ThroughDemoActivity" />
        <activity android:name=".activity.ScreenshotActivity" />

        <service android:name=".service.HowToStopService" />
    </application>

</manifest>